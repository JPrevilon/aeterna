import Image from "next/image";
import { PageHero } from "@/components/PageHero";
import { GiftCardSelector } from "@/components/GiftCardSelector";
import { meta } from "@/lib/seo";
export const metadata = meta({title:"Gift Cards", description:"Purchase Aeterna gift cards for luxury spa rituals, consultations, memberships, and wellness experiences in Hollywood, Florida.", path:"/gift-cards"});
const notes = ["Digital Stripe-ready checkout","Custom gifting note available after purchase","Usable toward eligible Aeterna services"];
export default function GiftCardsPage(){return <><PageHero eyebrow="Gift Cards" title="Give the gift of renewal." copy="A refined gift for birthdays, couples, holidays, bridal parties, and private wellness moments."/><section className="linen bg-cream px-6 py-16 md:px-8 lg:py-24"><div className="mx-auto grid max-w-7xl gap-10 lg:grid-cols-[.9fr_1.1fr]"><div className="relative overflow-hidden rounded-[3rem] border border-cocoa/10 bg-gradient-to-b from-cream to-vanilla/75 p-5 shadow-luxury md:p-8"><div className="floating-logo absolute -right-12 -top-12 h-52 w-52 opacity-[.06]"/><Image src="/assets/logos/aeterna-wordmark-logo.png" alt="Aeterna gift card" width={900} height={900} className="relative rounded-[2rem] border border-champagne/30 bg-cream shadow-inset"/><div className="relative mt-6 grid gap-3">{notes.map(note => <p key={note} className="rounded-full border border-cocoa/10 bg-cream/80 px-4 py-3 text-sm text-cocoa/70 shadow-inset">{note}</p>)}</div></div><div className="flex items-center"><div className="max-w-2xl"><p className="text-xs uppercase tracking-luxury text-sage">Digital Gift Cards</p><h2 className="mt-4 font-display text-5xl leading-tight text-espresso md:text-6xl">Choose an occasion.</h2><p className="mt-6 leading-8 text-cocoa/70">Select a curated amount or set a custom starting amount. The checkout button opens Stripe when payment is configured; otherwise it safely prompts guests to contact Aeterna.</p><div className="mt-8"><GiftCardSelector /></div></div></div></div></section></>}
