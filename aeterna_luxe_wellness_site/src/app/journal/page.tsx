import Link from "next/link";
import { PageHero } from "@/components/PageHero";
import { journalPosts } from "@/data/journal";
import { meta } from "@/lib/seo";
export const metadata = meta({title:"Journal", description:"Aeterna wellness journal with luxury spa guidance, IV therapy consultation education, medical weight-loss intake notes, and monthly wellness ritual planning.", path:"/journal"});
export default function JournalPage(){return <><PageHero eyebrow="Journal" title="The Aeterna Letter." copy="Wellness notes, seasonal rituals, treatment education, and private event announcements."/><section className="linen bg-cream px-6 py-16 md:px-8 lg:py-24"><div className="mx-auto grid max-w-6xl gap-6 md:grid-cols-3">{journalPosts.map(p=><Link href={`/journal/${p.slug}`} key={p.slug} className="group rounded-5xl border border-cocoa/10 bg-gradient-to-b from-cream to-vanilla/75 p-6 shadow-soft outline-none transition duration-500 hover:-translate-y-1 hover:border-champagne/40 hover:shadow-luxury focus-visible:ring-2 focus-visible:ring-sage/40 focus-visible:ring-offset-4 focus-visible:ring-offset-cream md:p-7"><p className="text-xs uppercase tracking-luxury text-sage">{p.category}</p><h2 className="mt-3 font-display text-4xl leading-tight text-espresso">{p.title}</h2><p className="mt-4 text-sm leading-7 text-cocoa/70">{p.excerpt}</p><div className="gold-line mt-8 transition duration-500 group-hover:opacity-100"/></Link>)}</div></section></>}
